{{- if not .Values.libraskefront.secrets.existingSecret }}
apiVersion: v1
kind: Secret
metadata:
  name: '{{ include "libraskefront.fullname" . }}-app-secret'
  labels:
    {{ include "libraskefront.labels" . | indent 4 }}
data:
{{- range $key, $val := .Values.libraskefront.secrets.appSecret }}
  {{ $key | quote }}: {{ $val | b64enc | quote }}
{{- end }}
{{- end }}
